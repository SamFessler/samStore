CREATE TABLE [dbo].[OrderProduct]
(
	[OrderID] INT NOT NULL,
	[ProductID] INT NOT NULL,
	Constraint[PK_ORderPRoduct] PrimaryKey ([ProductID)], ([OrderID]),
	Constraint[PK_orderProduct_Order] FOREIGN KEY (P
)
