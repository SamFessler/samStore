

<ul class="nav navbar-nav navbar-right">
    <li><a href="/cart"><span class="badge"></span>@(ViewBag.ItemsInCart ?? 0)<span class="glyphicon glyphicon-shopping-cart"></span>Cart</a></li>
    @if (User.Identity.IsAuthenticated)
    {
        <li>@Html.ActionLink("Logout as "+User.Identity.Name,"Logout","Account")</li>
        <li>@Html.ActionLink("Profile","Profile","Home")</li>
    }
    else
    {
        <li>@Html.ActionLink("Login", "Login", "Account")</li>
        <li>@Html.ActionLink("Register", "Register", "Account")</li>
        
    }

</ul>

